# Local dev overrides — loaded after .env to replace Docker-specific values
REDIS_URL=redis://localhost:6379/0
# NOTE: only use `//` vs `///` to get a relative path in Tortoise ORM
DATABASE_URL=sqlite://data/remander.db
